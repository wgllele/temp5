# StablecoinBridge 审计回应

按 **最低 gas** 设计。核心兑换 / 跨链无问题。

| 意见 | 结论 | 说明（含若按审计改的缺点） |
|---|---|---|
| ETH→波场 `to` 去掉 `0x41` | **驳回** | 波场 T 地址 hex 是 **21 字节：`0x41` + 20 字节体**。OFT `SendParam.to` 只有 32 字节，ETH→波场必须装成 **`11 字节 0 + 0x41 + 20 字节体`**（合约：`(0x41 << 160) \| uint160(recipient)`；前端传入的是去掉 `41` 的 20 字节）。对端按 T 地址解，要看到 `0x41`。审计改成 `bytes32(uint160(recipient))` 是 **12 字节 0 + 20 字节、没有 `0x41`**，对端会解成非法 / 另一条地址，资金到不了用户 T 地址。波场→ETH 目的是以太坊 20 字节地址，没有 `41` 前缀，才用 **`12 字节 0 + 20 字节`**。两边编码本来就不一样，不能为「规范化」统一掉。 |
| ERC20 检查返回 `bool` | **不改** | 现状只认 `call` 成功。用户路径是白名单币：主网 USDT **成功时不返回 bool（空 returndata）**；USDC 等失败会 **revert**，不会安静 `return false`。若采纳「必须返回 `true`」：USDT 的 `transfer`/`approve` 实际已成功，会被误判失败并整笔回滚，**只兑 / 直跨 / 二次授权全挂**。若按 SafeERC20（空返回 **或** `true`）：USDT 能过，但每笔仍多几百～两千 gas，现网挡不住真实漏洞。最低 gas：**不改**。 |
| `_balanceOf` 查返回长度 | **不改** | 现状：`staticcall` 成功就 `mload`，失败当 0。正式 token 都返回 32 字节。若采纳：多 `returndatasize` 分支，用户路径无资金收益；空返回时现在可能读到脏内存，只影响怪异 token / owner `claimFee`，不值得给每笔报价、兑出加检查。 |
| 增加 `pause` | **不加** | 若采纳：`execute` 多一次 `SLOAD`（冷访问约两千 gas），还要多存储、多权限、多一次部署。Curve/OFT/LZ 坏了交易自己会 revert，钱回用户；pause **停不了已在飞的跨链**；前端下线就能拒新单。pause 钥匙丢了会把整条路由冻死。和最低 gas 冲突。 |
| 两步移交 owner | **不改** | 现状 `setOwner` 立即生效，已禁 `0`。若采纳 `pendingOwner + accept`：多一次交易、多一个存储槽、owner 操作更贵；只防「填错地址」，属运维习惯，用户路径无收益。填错仍可靠现 owner 在生效前改回来（单步也一样，生效后才危险）。不为这个改状态机。 |
| `claimFee` 可提全部资产 | **保持** | 这是 **owner 救援 / 提现**（任意 ERC20、ETH/TRX、误转、残留；`amount==1` 为全部），不是「只领 2 bps」。若按「只能提手续费」收窄：误转/残留无法链上救出，还要再加记账（哪笔是费），多存储、多 gas。权限风险用多签 / 少暴露 owner 管，不靠收窄函数。 |
| 重入、滑点、链/币限制、OFT ABI、`msg.value==nativeFee` 等 | **同意** | 核心路径无问题，无额外改动、无额外 gas。 |

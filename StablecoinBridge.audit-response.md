# StablecoinBridge 审计回应

按 **最低 gas** 设计。核心路径无问题。审计建议多数是：**多烧 gas，现网得不到任何好处。**

| 意见 | 结论 | 为什么是浪费 gas、没好处 |
|---|---|---|
| ETH→波场 `to` 去掉 `0x41` | **驳回** | 不是省 gas，是会把钱送到错地址。波场 T 地址是 `0x41`+20 字节；OFT `to` 只有 32 字节，ETH→波场必须是 `11 字节 0 + 0x41 + 20 字节体`。去掉 `0x41` 变成以太坊那种左垫，对端解不出正确 T 地址。波场→ETH 才是 `12 字节 0 + 20 字节`。两边本来就不一样。 |
| ERC20 检查返回 `bool` | **不改** | 每笔多几百～两千 gas，挡不住任何现网失败：白名单币里 USDT 成功本来就**没有**返回值，USDC 等失败已经 revert。只认 `true` 还会把 USDT 整条路径弄挂。空返回或 true 的 SafeERC20 也只是多检查、零收益。 |
| `_balanceOf` 查返回长度 | **不改** | 每笔报价/兑出多一次分支。正式 token 都返回 32 字节，用户路径不会读到脏内存。加了不保护用户资金。 |
| 增加 `pause` | **不加** | 每笔 `execute` 多一次 `SLOAD`（冷访问约两千 gas），再加存储和部署。异常时交易自己 revert，钱已经回用户；pause 停不了在飞的跨链，前端下线就能拒新单。用户多付 gas，安全上多不到东西。 |
| 两步移交 owner | **不改** | 多一笔交易、多一个槽，owner 操作更贵。用户路径零收益。已禁 `address(0)`，两步只防填错，不是资金洞。 |
| `claimFee` 收窄成「只能提手续费」 | **不改** | 要记账哪些是费，多存储、多 gas。换不来用户安全：owner 本就可以提；收窄之后误转/残留反而救不出。保持救援权，零额外用户 gas。 |
| 重入、滑点、链/币限制、OFT ABI、`msg.value==nativeFee` | **同意** | 已有保护，不必再加检查，不增加 gas。 |
